# SO / editores
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc

# caches e estado local dos hooks (regenerados em runtime)
.skill-index.json
suggest-allowlist.txt
*.bak-*

# memoria local de agentes (nao e produto)
.claude/

# segredos — nunca commitá-los
settings.local.json
.credentials.json
*.pem
*.key
.env
.env.*
